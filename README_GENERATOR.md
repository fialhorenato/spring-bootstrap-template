This repository now includes a Yeoman generator (generator-spring-bootstrap).

Install locally for development:
  npm install
  npm link
  npm link generator-spring-bootstrap
  yo spring-bootstrap

The generator clones this repository as a project template and applies chosen configuration (package name, groupId, artifactId, database choice, Docker, git init, optional build verification).
